import {
	DownloadLinkContainer,
	type DownloadLinkContainerProps,
} from "./container";

export const DownloadLink = (props: DownloadLinkContainerProps) => (
	<DownloadLinkContainer {...props} />
);
