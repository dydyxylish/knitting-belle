import {
	AuthOrCheckOutContainer,
	type AuthOrCheckOutContainerProps,
} from "./container";

export const AuthOrCheckOut = (props: AuthOrCheckOutContainerProps) => (
	<AuthOrCheckOutContainer {...props} />
);
