import {
	OrderDetailImageContainer,
	type OrderDetailImageContainerProps,
} from "./container";

export const OrderDetailImage = ({
	knittingPattern,
}: OrderDetailImageContainerProps) => (
	<OrderDetailImageContainer knittingPattern={knittingPattern} />
);
