import {
	DownloadButtonContainer,
	type DownloadButtonContainerProps,
} from "./container";

export const DownloadButton = (props: DownloadButtonContainerProps) => (
	<DownloadButtonContainer {...props} />
);
